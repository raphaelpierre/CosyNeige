# Database - PostgreSQL
# Production: Use local PostgreSQL
# Development: You can use local or remote (NeonDB, etc.)
DATABASE_URL="postgresql://username:password@localhost:5432/database_name"

# Google Search Console Verification
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION="your-google-verification-code"

# Authentication Secrets
# Generate with: openssl rand -base64 32
JWT_SECRET="your-jwt-secret-key-here"
NEXTAUTH_SECRET="your-nextauth-secret-key-here"

# Email Configuration
ADMIN_EMAIL="contact@your-domain.com"

# SMTP Configuration
# Production: Use localhost:25 for self-hosted Postfix
# Development: Use external SMTP or localhost if Postfix installed
SMTP_HOST="localhost"
SMTP_PORT="25"
SMTP_FROM="noreply@your-domain.com"

# Optional SMTP Authentication (if using external SMTP)
# SMTP_USER="your-smtp-username"
# SMTP_PASS="your-smtp-password"

# App Configuration
NEXT_PUBLIC_BASE_URL="https://your-domain.com"
NEXTAUTH_URL="https://your-domain.com"

# Stripe Payment Configuration
# Get your keys from: https://dashboard.stripe.com/apikeys
# Use test keys (pk_test_...) for development
# Use live keys (pk_live_...) for production
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_your-publishable-key"
STRIPE_SECRET_KEY="sk_live_your-secret-key"

# Stripe Webhook Secret (optional, for webhook verification)
# Get from: https://dashboard.stripe.com/webhooks
# STRIPE_WEBHOOK_SECRET="whsec_your-webhook-secret"
